{
  "name": "my-navigation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.5.0"
  },
  "scripts": {
    "start": "node --use-env-proxy src/server.js",
    "test": "node --test tests/*.test.js",
    "test:ui": "node tests/browser.mjs",
    "check": "node --check src/app.js && node --check src/database.js && node --check src/schema.js && node --check src/weather.js && node --check public/home.js && node --check public/admin.js && node --check public/ui.js && node --check public/appearance.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.0",
    "helmet": "^8.1.0",
    "lucide-static": "^1.41.0",
    "multer": "^2.0.2",
    "sharp": "^0.35.4",
    "sortablejs": "^1.15.7",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
